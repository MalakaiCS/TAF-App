project_id = "djexdkwohkylunnwbxpf"

# Reads uploaded purchase orders. Called by the desktop app with the signed-in
# user's token, so JWT verification stays on (the default).
[functions.extract-orders]
verify_jwt = true

# The phone upload page is not a function: it is published to a public
# Storage bucket by the app (see migrate_phone_page.sql), because Storage
# returns exactly the content type set at upload.
